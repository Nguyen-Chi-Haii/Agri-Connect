<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:padding="@dimen/spacing_lg"
    tools:context=".ui.auth.RoleSelectionActivity">

    <!-- Title Section -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_xxl"
        android:text="@string/select_role_title"
        android:textColor="@color/text_primary"
        android:textSize="@dimen/text_xxl"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tvSubtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_sm"
        android:text="@string/select_role_subtitle"
        android:textColor="@color/text_secondary"
        android:textSize="@dimen/text_body"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvTitle" />

    <!-- Role Cards Container -->
    <LinearLayout
        android:id="@+id/cardsContainer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_xl"
        android:orientation="vertical"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvSubtitle"
        app:layout_constraintBottom_toTopOf="@id/btnContinue"
        app:layout_constraintVertical_bias="0.3">

        <!-- Farmer Card -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cardFarmer"
            android:layout_width="match_parent"
            android:layout_height="@dimen/role_card_height"
            android:layout_marginBottom="@dimen/spacing_md"
            android:clickable="true"
            android:focusable="true"
            app:cardCornerRadius="@dimen/radius_xl"
            app:cardElevation="@dimen/elevation_sm"
            app:cardBackgroundColor="@color/surface"
            app:strokeColor="@color/border"
            app:strokeWidth="1dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:orientation="vertical"
                android:padding="@dimen/spacing_lg">

                <ImageView
                    android:id="@+id/ivFarmer"
                    android:layout_width="@dimen/icon_role"
                    android:layout_height="@dimen/icon_role"
                    android:src="@drawable/ic_farmer"
                    android:contentDescription="@string/role_farmer" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/spacing_md"
                    android:text="@string/role_farmer"
                    android:textColor="@color/farmer_green"
                    android:textSize="@dimen/text_lg"
                    android:textStyle="bold" />

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/spacing_xs"
                    android:gravity="center"
                    android:paddingHorizontal="@dimen/spacing_sm"
                    android:text="@string/role_farmer_desc"
                    android:textColor="@color/text_secondary"
                    android:textSize="@dimen/text_sm"
                    android:maxLines="2"
                    android:ellipsize="end" />
            </LinearLayout>

            <!-- Selection Indicator -->
            <ImageView
                android:id="@+id/ivFarmerCheck"
                android:layout_width="@dimen/icon_md"
                android:layout_height="@dimen/icon_md"
                android:layout_gravity="top|end"
                android:layout_margin="@dimen/spacing_md"
                android:src="@drawable/ic_check_circle"
                android:visibility="gone"
                android:contentDescription="Selected"
                app:tint="@color/farmer_green" />
        </com.google.android.material.card.MaterialCardView>

        <!-- Trader Card -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cardTrader"
            android:layout_width="match_parent"
            android:layout_height="@dimen/role_card_height"
            android:clickable="true"
            android:focusable="true"
            app:cardCornerRadius="@dimen/radius_xl"
            app:cardElevation="@dimen/elevation_sm"
            app:cardBackgroundColor="@color/surface"
            app:strokeColor="@color/border"
            app:strokeWidth="1dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:orientation="vertical"
                android:padding="@dimen/spacing_lg">

                <ImageView
                    android:id="@+id/ivTrader"
                    android:layout_width="@dimen/icon_role"
                    android:layout_height="@dimen/icon_role"
                    android:src="@drawable/ic_trader"
                    android:contentDescription="@string/role_trader" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/spacing_md"
                    android:text="@string/role_trader"
                    android:textColor="@color/trader_blue"
                    android:textSize="@dimen/text_lg"
                    android:textStyle="bold" />

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/spacing_xs"
                    android:gravity="center"
                    android:paddingHorizontal="@dimen/spacing_sm"
                    android:text="@string/role_trader_desc"
                    android:textColor="@color/text_secondary"
                    android:textSize="@dimen/text_sm"
                    android:maxLines="2"
                    android:ellipsize="end" />
            </LinearLayout>

            <!-- Selection Indicator -->
            <ImageView
                android:id="@+id/ivTraderCheck"
                android:layout_width="@dimen/icon_md"
                android:layout_height="@dimen/icon_md"
                android:layout_gravity="top|end"
                android:layout_margin="@dimen/spacing_md"
                android:src="@drawable/ic_check_circle"
                android:visibility="gone"
                android:contentDescription="Selected"
                app:tint="@color/trader_blue" />
        </com.google.android.material.card.MaterialCardView>
    </LinearLayout>

    <!-- Continue Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnContinue"
        android:layout_width="0dp"
        android:layout_height="@dimen/button_height"
        android:layout_marginBottom="@dimen/spacing_lg"
        android:text="@string/continue_btn"
        android:textAllCaps="false"
        android:textSize="@dimen/text_md"
        android:enabled="false"
        app:cornerRadius="@dimen/radius_lg"
        app:layout_constraintBottom_toTopOf="@id/tvLogin"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Login Link -->
    <LinearLayout
        android:id="@+id/tvLogin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/spacing_lg"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/have_account"
            android:textColor="@color/text_secondary"
            android:textSize="@dimen/text_body" />

        <TextView
            android:id="@+id/tvLoginLink"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/spacing_xs"
            android:text="@string/login"
            android:textColor="@color/primary"
            android:textSize="@dimen/text_body"
            android:textStyle="bold" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
